<%=form_for @category do |f| %>
	<%= f.text_field :name, placeholder: 'Category name', class: "form-control"%><br>
	<%= f.text_area :description, placeholder: 'Description (Optional)', class: "form-control" %><br>
	<%= f.submit(btn_text, class: "btn btn-large btn-primary") %>
<% end %>